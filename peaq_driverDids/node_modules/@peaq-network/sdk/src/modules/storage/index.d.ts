import { ApiPromise } from '@polkadot/api';
import type { ISubmittableResult } from '@polkadot/types/types';
import type { SDKMetadata } from '../../types';
import { Base } from '../base';
import { AddItemOptions, RemoveItemOptions, GetItemOptions, UpdateItemOptions, AddItemResult, RemoveItemResult, GetItemResult, UpdateItemResult, EvmTransaction } from './interface';
export declare class Storage extends Base {
    protected readonly _api?: ApiPromise | undefined;
    protected readonly _metadata?: SDKMetadata | undefined;
    constructor(_api?: ApiPromise | undefined, _metadata?: SDKMetadata | undefined);
    /**
     * Adds item to peaqStorage
     *
     * TODO Determine what constraints we need to establish before adding to storage
     */
    addItem(options: AddItemOptions, statusCallback?: (result: ISubmittableResult) => void | Promise<void>): Promise<AddItemResult | EvmTransaction>;
    /**
     * Removes the itemType in peaqStorage
     */
    removeItem(options: RemoveItemOptions, statusCallback?: (result: ISubmittableResult) => void | Promise<void>): Promise<RemoveItemResult | EvmTransaction>;
    getItem(options: GetItemOptions): Promise<GetItemResult | null>;
    updateItem(options: UpdateItemOptions, statusCallback?: (result: ISubmittableResult) => void | Promise<void>): Promise<UpdateItemResult | EvmTransaction>;
    private _checkSeed;
    private _checkAddress;
}
