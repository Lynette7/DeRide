/// <reference types="bn.js" />
import { BN } from '@polkadot/util';
import { KeyringPair } from '@polkadot/keyring/types';
import { ApiPromise } from '@polkadot/api';
import { SubmittableExtrinsic } from '@polkadot/api/types';
import type { Address, PeaqEvent, PeaqEventData, SignTransction } from '../../types';
export declare class Base {
    protected _api?: ApiPromise | undefined;
    private _nonceStore;
    constructor(_api?: ApiPromise | undefined);
    protected _getApi(): ApiPromise;
    protected _getKeyPair: (seed: string) => KeyringPair;
    protected _getNonce(address: Address): Promise<BN>;
    protected _validateInput(input: string): boolean;
    protected _serializeTx(api: ApiPromise, tx: SubmittableExtrinsic<'promise'>): Promise<string>;
    protected _transactionError(method: string, eventData: PeaqEventData): Promise<{
        documentation: string[];
        name: string;
        section: string;
    } | null>;
    protected _newSignTx(option: SignTransction): Promise<PeaqEvent[]>;
}
